<h1 class="products-title">Products</h1>
{{#each model as |product|}}

        <h3>{{product.title}}</h3>
        <img style="max-width:180px;" src="{{product.image}}" alt="">
        <p>{{product.desc}}</p>

{{/each}}
{{outlet}}
